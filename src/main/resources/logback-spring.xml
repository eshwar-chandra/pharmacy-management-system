<configuration>
    <springProperty scope="context" name="springAppName" source="spring.application.name" defaultValue="pharmacy-management"/>

    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <providers>
                <timestamp>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <version/> <!-- Logback version -->
                <logLevel/>
                <loggerName/>
                <threadName/>
                <message/>
                <stackTrace/>
                <mdc/> <!-- Important: include MDC properties -->
                <context/>
                <springAppName/>
            </providers>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE_JSON"/>
    </root>

    <!-- Example of setting a specific logger level, can be managed in application.yml too -->
    <logger name="com.pharmacy.management" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE_JSON"/>
    </logger>
    <logger name="org.springframework.web" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE_JSON"/>
    </logger>
    <logger name="org.hibernate.SQL" level="WARN" additivity="false">
       <appender-ref ref="CONSOLE_JSON"/>
    </logger>
    <logger name="org.hibernate.type.descriptor.sql" level="WARN" additivity="false">
       <appender-ref ref="CONSOLE_JSON"/>
    </logger>

</configuration>
